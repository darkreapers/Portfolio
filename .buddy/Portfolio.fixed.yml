- pipeline: "Portfolio"
  events:
  - type: "PUSH"
    refs:
    - "refs/heads/main"
  fail_on_prepare_env_warning: true
  actions:
  - action: "python manage.py test"
    type: "BUILD"
    docker_image_name: "library/python"
    docker_image_tag: "3.10.6"
    execute_commands:
    - "pip install -r requirements.txt"
    - "python manage.py test"
    cached_dirs:
    - "/root/.cache/pip"
    shell: "BASH"
